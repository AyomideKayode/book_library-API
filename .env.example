# Book Library API Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration
# Get your MongoDB connection string from MongoDB Atlas
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>
# Example: MONGODB_URI=mongodb+srv://bookuser:mypassword@cluster0.abcdef.mongodb.net/book-library-db

MONGODB_URI=your_mongodb_connection_string_here

# MongoDB Atlas Setup Instructions:
# 1. Go to https://www.mongodb.com/cloud/atlas
# 2. Create a free account or sign in
# 3. Create a new cluster (free tier available)
# 4. Create a database user with read/write permissions
# 5. Add your IP address to the network access list (or use 0.0.0.0/0 for all IPs)
# 6. Get your connection string from the "Connect" button
# 7. Replace <password> with your database user password
# 8. Replace <dbname> with your preferred database name (e.g., book-library-db)

# Local MongoDB (Alternative)
# If you prefer to use a local MongoDB instance:
# MONGODB_URI=mongodb://localhost:27017/book-library-db

# Additional Configuration (Optional)
# API_VERSION=v1
# LOG_LEVEL=info
# CORS_ORIGIN=*
